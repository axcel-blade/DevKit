# Plugins

Built-in plugins in **0.3.0**: `git`, `flutter`, `composer`, `jdk`, `mono`, `hello`.

```bash
python main.py list
python main.py install git
python main.py install jdk
```

Authoring guide: [docs/plugins.md](../docs/plugins.md).
